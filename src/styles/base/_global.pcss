html {
  box-sizing: border-box;
  height: 100%;
}

*, *::before, *::after {
  box-sizing: inherit;
}

body {
  height: 100%;
  margin: 0;
  color: $font;
  font-family: Raleway;
  background-color: $mainbg;
}

